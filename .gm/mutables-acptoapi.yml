- id: acptoapi-spawn-return-value
  claim: spawn_acptoapi_daemon() modified to return u32 (PID) instead of () and child captured
  witness_method: Read C:\dev\rs-plugkit\src\hook\session_start.rs spawn_acptoapi_daemon function
  witness_evidence: "session_start.rs:324-347 shows fn spawn_acptoapi_daemon() -> std::io::Result<u32>. Lines 334 and 344 capture child with let child = cmd.spawn()?. Line 335 and 345 return Ok(child.id()). Modification complete."
  status: witnessed

- id: tools-dir-path-available
  claim: super::tools_dir() function exists and is used to store acptoapi PID
  witness_method: Read C:\dev\rs-plugkit\src\hook\mod.rs and session_start.rs usage
  witness_evidence: "mod.rs:37-42 defines pub fn tools_dir() -> PathBuf. session_start.rs:297 uses super::tools_dir().join(\".acptoapi-pid\") to store PID file. session_end.rs:72 uses same path to retrieve PID. Function available and used correctly."
  status: witnessed

- id: kill-tree-import-session-end
  claim: rs_exec::kill::kill_tree is accessible and used in session_end for cleanup
  witness_method: Read C:\dev\rs-plugkit\src\hook\session_end.rs
  witness_evidence: "session_end.rs:85 calls rs_exec::kill::kill_tree(pid) to terminate acptoapi process. No explicit import needed; available via rs_exec crate. Function properly integrated into kill_acptoapi_if_running() at lines 71-87."
  status: witnessed
