- id: thebird-auto-load-gm-plugin
  subject: Auto-load gm plugkit plugin in thebird bootHost
  status: pending
  description: Subagent witness confirmed host.pi.hooks=0 on deployed page until plugin manually calls loadPlugin. Original ask requires gm actually wired in-browser. Edit C:/dev/thebird/dist/app/ host bootstrap to auto-load gm plugin (plugsdk createHost + loadClaudePlugin from AnEntrypoint/gm-thebird bin/plugkit.wasm) at instance creation. Witness via deployed page page.evaluate that host.pi.hooks > 0 immediately after boot.
  effort: medium
  category: feature
  route_family: boundary
  load: 0.7
  authorization: none
  blockedBy: []
  acceptance:
    - page.evaluate after fresh load shows host.pi.hooks length > 0 without manual loadPlugin
    - send any chat message; window.__debug.instances[id].host hook log shows preToolUse fired
