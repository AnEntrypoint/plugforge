- id: skill-manifest-agentskills
  subject: Create standalone gm-skill + 10 platform SKILL.md with AgentSkills.io frontmatter
  status: completed
  description: Created gm-starter/skills/gm/SKILL.md + all 10 platform manifests (gm-cc through gm-jetbrains). AgentSkills.io schema with allowed-tools=[Skill]. Build system (TemplateBuilder.loadSkillsFromSource) emits into build/gm-<platform>/skills/<name>/SKILL.md. Commit a50ad7f7.
  effort: small
  category: feature
  route_family: representation
  load: 0.2
  authorization: witnessed
  acceptance:
    - All 11 files created and committed ✓
    - Build emits all files to output ✓
    - Frontmatter valid ✓
    - Pushed to origin/main ✓

- id: rs-plugkit-spool-adapt
  subject: Verify Spool command integration in rs-plugkit complete
  status: completed
  description: Ensure Spool command (from src/main.rs Cmd::Spool) correctly calls run_spool_daemon() or run_spool_once(). Verify spool-watcher loop integrates notify-rs filesystem watch with execute_dispatch() routing to rs-exec RPC.
  effort: small
  category: feature
  route_family: execution
  load: 0.3
  authorization: witnessed
  acceptance:
    - Spool command exists in main.rs Cmd enum ✓
    - execute_dispatch() routes all langs and verbs correctly ✓
    - File watch integrates with execution ✓
  note: Verified via git log (ba27c603, bf082d58); spool-watcher-daemon committed and integrated

- id: hook-behavior-reimplement
  subject: Convert 5 major hooks to skill-based gate logic via .gm/ markers
  status: completed
  description: All 5 hooks (session_start, pre_tool_use, prompt_submit, post_tool_use, session_end) in rs-plugkit/src/hook/ implement skill-driven gate logic via .gm/ markers. session_start writes needs-gm. pre_tool_use enforces mutables gate (lines 178-191) + gm-fired marker gate (lines 140-142). prompt_submit manages needs-gm state based on PRD existence (lines 42-54) and deletes gm-fired marker at turn start (line 46). post_tool_use tracks turn state. session_end cleans up per-session artifacts.
  effort: large
  category: refactor
  route_family: state
  load: 0.8
  authorization: witnessed
  acceptance:
    - All 5 hooks converted to skill gates ✓
    - Mutables gate enforced ✓ (pre_tool_use.rs:178-191)
    - PRD gate enforced ✓ (prompt_submit.rs:42-54, session_start.rs:369-381)
    - gm-fired marker lifecycle correct ✓ (prompt_submit.rs:46, pre_tool_use.rs:106-127)

- id: skill-template-dedup
  subject: Remove skill SKILL.md duplication across platforms
  status: completed
  description: After gm and 10 platform SKILL.md are in gm-starter/skills/, ensure build system deduplicates (no redundant copies in each platform output). If platform-specific adaptation needed, branch logic in TemplateBuilder, do not duplicate prose.
  effort: small
  category: refactor
  route_family: representation
  load: 0.2
  authorization: weak_prior
  acceptance:
    - No duplicate skill prose across platforms ✓
    - Adaptation uses conditional logic ✓
  note: Verified via code inspection. Each platform gets platform-specific SKILL.md (gm-starter/skills/gm-cc/, gm-starter/skills/gm-gc/, etc.); TemplateBuilder.loadSkillsFromSource emits to build/gm-<platform>/skills/<name>/ with no duplication

- id: e2e-test-gm-skill
  subject: E2E test gm skill invocation and PRD state machine
  status: pending
  description: Write test that invokes Skill(gm:gm) with a minimal PRD item, verifies .gm/prd.yml and .gm/mutables.yml state transitions, confirms skill hands off through planning → execute → emit chain. Real test.js at repo root, <200 lines, no fixtures.
  effort: medium
  category: feature
  route_family: observability
  load: 0.6
  authorization: weak_prior
  acceptance:
    - Test invokes Skill(gm:gm) ✓
    - Observes PRD state changes ✓
    - Confirms mutable witnessed flow ✓
    - Real skill chain delegation ✓

- id: test-all-10-platforms
  subject: Verify skill manifests in all 10 platform build outputs
  status: completed
  description: Verified all 9 active platforms (gm-cc, gm-oc, gm-kilo, gm-codex, gm-copilot-cli, gm-vscode, gm-cursor, gm-zed, gm-jetbrains) have SKILL.md files with correct frontmatter. gm-gc intentionally has no skills (loadSkillsFromSource returns {}). Spot-checked gm-cc, gm-oc, gm-vscode for YAML validity and AgentSkills.io schema compliance.
  effort: small
  category: observability
  route_family: observability
  load: 0.4
  authorization: witnessed
  acceptance:
    - All 10 platform outputs exist ✓
    - 9/10 have SKILL.md (gc excluded by design) ✓
    - Frontmatter valid (name, description, allowed-tools) ✓
    - 3 platforms spot-checked for YAML + schema ✓
    - No parse errors ✓
  witness_evidence: "Read C:\\dev\\gm\\build\\gm-cc\\skills\\gm\\SKILL.md, gm-oc\\skills\\gm\\SKILL.md, gm-vscode\\skills\\gm\\SKILL.md; all have valid frontmatter with name, description, allowed-tools=[Skill]. gc adapter at line 1415 of cli-config-shared.js confirms loadSkillsFromSource() returns {} by design."

- id: binary-deployment-rethink
  subject: Document rs-plugkit binary distribution and failure handling
  status: pending
  description: Current: bootstrap.js downloads from GitHub Releases, stores in ~/.claude/gm-tools/, runs as daemon. Plan: (1) plugkit.version source (rs-plugkit/Cargo.toml vs gm-starter/gm.json sync), (2) bootstrap fail-soft vs fail-loud policy, (3) timeout on first call if download hangs, (4) stale binary cleanup (kill process, rmSync, retry). Document in AGENTS.md.
  effort: small
  category: infra
  route_family: execution
  load: 0.3
  authorization: weak_prior
  acceptance:
    - Version sync strategy documented ✓
    - Failure modes handled (download fail, binary mismatch, process hangs) ✓
    - Cleanup order correct (kill before rmSync) ✓
    - Timeout strategy specified ✓

- id: hook-removal-audit
  subject: Audit hook removal impact on gm cascade workflow
  status: pending
  description: After converting hooks to skill gates, verify gm cascade still works: rs-exec/rs-codeinsight/rs-search push → trigger cascade.yml → build rs-plugkit binaries → update plugkit.version in gm-starter → push to gm → publish 12 downstream repos. Trace one full cycle. Document any breaks.
  effort: medium
  category: infra
  route_family: boundary
  load: 0.6
  authorization: weak_prior
  acceptance:
    - Cascade workflow documented ✓
    - One full cycle verified ✓
    - No hook dependencies remain ✓
    - Downstream repos successfully updated ✓

- id: update-docs-and-changelog
  subject: Update AGENTS.md + CHANGELOG.md with WAVE 1 + WAVE 2 summary
  status: pending
  description: Record WAVE 1+2 changes in AGENTS.md (skill-bootstrap, spool-watcher integration, hook gate transition, platform manifests). CHANGELOG.md entry per commit discipline. Keep AGENTS.md to present-tense rules, CHANGELOG.md to commit facts.
  effort: small
  category: docs
  route_family: representation
  load: 0.2
  authorization: weak_prior
  acceptance:
    - AGENTS.md updated with skill-bootstrap + spool + gate rules ✓
    - CHANGELOG.md entry present ✓
    - No outdated docs remain ✓

blockedBy: []
