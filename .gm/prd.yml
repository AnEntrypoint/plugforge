- id: browser-e2e-todolist
  subject: End-to-end witness creating a todolist app via freddie+gm in thebird
  status: in_progress
  description: Browser drives freddie chat - create a todolist app, observe app render in preview iframe, witness persistence, reload, items survive. Captures hook trajectory in window.__debug.gm.
  effort: medium
  category: feature
  route_family: observability
  load: 0.8
  authorization: none
  acceptance:
    - Preview iframe contains todolist app with persisted items after reload

- id: freddie-in-page-server
  subject: Run freddie agent runtime inside the thebird browser tab
  status: pending
  description: Currently thebird's `freddie-chat.js` and `freddie-host.js` only bridge gm hooks; the full freddie agent loop (xstate machine, LLM dispatch, tool execution) runs out-of-page. Embed freddie's runtime so chat:send drives the actual freddie machine in-page (with the existing gm plugkit auto-loaded). Reuse the freddie source from C:/dev/freddie/src — bundle via esbuild into thebird/dist/app/freddie-runtime.js or load over a CDN-friendly bundle. Wire instance.host.runCli('run', text) to actually invoke freddie.machine.send. Witness: live page chat triggers freddie state transitions (page.evaluate window.__debug.freddie.machine.state) and gm hook trajectory grows on each turn.
  effort: large
  category: feature
  route_family: boundary
  load: 0.85
  authorization: none
  blockedBy: []
  acceptance:
    - https://anentrypoint.github.io/thebird/ live page exposes window.__debug.freddie.machine with state machine
    - chat.send('hello') triggers freddie machine transition (state delta witnessed via page.evaluate)
    - each freddie turn appends to window.__debug.gm.trajectory (hooks fire from the in-page freddie loop)
