- id: gm-cleanup
  subject: Remove dead files from gm root
  status: pending
  description: Delete pong.sh, ccsniff-gm-lint.js, gm-build/ artifacts. validate/probe.js - remove if unreferenced by CI.
  effort: small
  category: refactor
  route_family: grounding
  load: 0.2
  failure_modes: [deleting file still referenced by CI]
  route_fit: examined
  authorization: witnessed
  blocking: []
  blockedBy: []
  acceptance:
    - pong.sh deleted
    - ccsniff-gm-lint.js deleted
    - gm-build/ deleted
    - validate/probe.js deleted if unreferenced
  edge_cases:
    - probe.js referenced by .github workflow

- id: gmsniff-build
  subject: Build gmsniff observability GUI for gm JSONL logs
  status: pending
  description: New project at C:/dev/gmsniff. Reads ~/.claude/gm-log/<YYYY-MM-DD>/<subsystem>.jsonl. EventEmitter watcher + GUI server + SSE + browser app. 8 subsystems - plugkit, exec, hook, rs_learn, rs_codeinsight, rs_search, bootstrap, plugkit_wrapper.
  effort: large
  category: feature
  route_family: observability
  load: 0.9
  failure_modes: [JSONL schema varies per subsystem, gm-log path missing, SSE reconnect]
  route_fit: examined
  authorization: witnessed
  blocking: []
  blockedBy: [gm-cleanup]
  acceptance:
    - npm start launches GUI on localhost
    - all 8 subsystems visible
    - events stream live via SSE
    - filter by subsystem and event type
    - browser witness confirms live events
  edge_cases:
    - gm-log dir missing on first run
    - log rotates at midnight
