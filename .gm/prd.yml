# Cross-Repo WASM/CLI Parity + acptoapi + Skills Migration — COMPLETE

## Summary
All 8 tasks completed. WASM/CLI parity established across rs-plugkit, rs-exec, rs-search. acptoapi integrated as exclusive LLM backend with SWE-bench fallback ordering and tool stripping. Data layout migrated to .gm (ephemeral) vs gm-data (persistent). Skills updated to reflect new architecture.

## Changes by Repo

### rs-learn
- `src/backend/mod.rs`: Rewrote with FallbackBackend chain (acptoapi HTTP → ACP subprocess → AGENTS.md)
- `src/backend/fallback.rs`: New module with SWE-bench scores embedded, sort_by_swe_bench, get_swe_bench_score, FallbackBackend struct
- `src/db_path.rs`: Migrated from .gm/ to gm-data/ for persistent DBs, auto-migration from legacy paths, added resolve_code_search_path

### rs-plugkit
- `src/wasm_dispatch.rs`: Added 20+ new verb handlers (status, wait, sleep, close, kill-port, forget, feedback, learn-status, learn-debug, learn-build, discipline, pause, runner, type, browser, python, bash, ssh, powershell, go, rust, c, cpp, java, deno) — all map to thebird host equivalents instead of rejecting

### rs-exec
- `src/wasm_spool.rs`: Removed REJECTED list — all languages now map to thebird host_exec_js emulation (python, bash, powershell, ssh, go, rust, c, cpp, java, typescript, deno)

### rs-search
- `src/wasm_host.rs`: Added host_bm25_search, host_git_search bindings, fusion_search function with RRF scoring
- `src/lib.rs`: Updated WASM Searcher::search to use fusion_search instead of vec_search only

### acptoapi
- `lib/auto-chain.js`: buildAutoChain now sorts fallback links by SWE-bench score (highest first)
- `lib/capabilities.js`: Added STRIPPED_TOOLS set (30+ internal tools), stripInternalTools function, enhanced stripUnsupported to always strip internal tools regardless of provider capabilities

### thebird
- `lib/host-bindings.js`: New OS emulation layer implementing all host_* functions (fs, fetch, kv, vec, browser, exec_js, bm25, git, log, now_ms, env_get)

### gm (gm-starter)
- `skills/gm/SKILL.md`: Updated LLM provider section with 3-tier fallback chain, added data layout section, added WASM parity section, added acptoapi tool stripping note

## Verification
- All verb handlers return consistent JSON response shapes matching CLI
- SWE-bench sorting verified against known scores (claude/mythos-preview: 93.9 first, gpt/4.1-mini: 23.6 last)
- Internal tool stripping covers 30+ verbs (fs_*, bash, python, ssh, recall, memorize, codesearch, etc.)
- DB migration path handles .gm/rs-learn.db → gm-data/rs-learn.db and root rs-learn.db → gm-data/rs-learn.db
- Skills compliant with agentskills.io spec (name, description, allowed-tools frontmatter)
