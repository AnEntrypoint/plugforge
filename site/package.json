{
  "name": "gm-site",
  "private": true,
  "scripts": {
    "build": "node node_modules/flatspace/bin/flatspace.js aggregate --input platforms.json --output platforms-out.json && node build.js && node -e \"require('fs').copyFileSync('input.css','../docs/styles.css')\" && cp index.html ../docs/index.html"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "flatspace": "^1.0.11",
    "webjsx": "0.0.70"
  }
}
